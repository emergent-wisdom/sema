{
  "name": "sema",
  "version": "0.1.12",
  "description": "Content-addressed vocabulary protocol — shared meaning for AI agents",
  "skills": "./skills/",
  "userConfig": {
    "allow_mint": {
      "title": "Allow Minting",
      "description": "Allow minting new sema patterns (true/false, default: false)",
      "type": "string",
      "sensitive": false
    }
  },
  "mcpServers": {
    "sema": {
      "command": "uvx",
      "args": ["--from", "semahash[mcp]", "sema", "mcp"],
      "env": {
        "SEMA_ALLOW_MINT": "${user_config.allow_mint}"
      }
    }
  }
}
