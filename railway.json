{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "nixpacksConfigPath": "nixpacks.toml",
    "buildCommand": "pip install -e '.[api]' && cd web && npm install && npm run build && mkdir -p ../src/sema/server/static && cp -r dist/* ../src/sema/server/static/"
  },
  "deploy": {
    "startCommand": "sema serve --host 0.0.0.0 --port $PORT",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
